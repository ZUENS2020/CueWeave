CueWeave 0.1 — Windows 11 x64

解压后应得到一个名为 CueWeave 的文件夹。
进入该文件夹，双击 CueWeave.Windows.exe。
不要把 exe 单独拷走，旁边的 DLL / WinApp SDK 文件都要留着。

已自包含 .NET 运行时和 Windows App SDK，不必再装运行库。

---

Unzip to get one folder named CueWeave.
Open that folder and run CueWeave.Windows.exe.
Do not copy the exe somewhere else; the files next to it are required.

Self-contained: no extra .NET or Windows App SDK install.
Requires Windows 11 x64.
